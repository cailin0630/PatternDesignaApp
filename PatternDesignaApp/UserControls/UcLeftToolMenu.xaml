<UserControl x:Class="PatternDesignaApp.UserControls.UcLeftToolMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PatternDesignaApp.UserControls"
             mc:Ignorable="d" 
             >
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"></Setter>
        </Style>
    </UserControl.Resources>
    <Border BorderThickness="1" BorderBrush="Brown">
        <StackPanel>
            <Button Content="选择区域"></Button>
            <Button Content="直线"
                    Command="{Binding SelectHbCommand}"></Button>
            <Button Content="矩形"
                    Command="{Binding SelectDbxCommand}"></Button>
            <Button Content="圆"
                    Command="{Binding SelectYCommand}"></Button>
            <Button Content="填充"></Button>
            <Button Content="取色"></Button>
            <Button Content="编辑"></Button>
        </StackPanel>
    </Border>
   
</UserControl>
